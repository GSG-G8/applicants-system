import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  profile: {
    marginRight: '10px',
  },
  logout: {
    marginRight: '10px',
  },
});

export default useStyles;
