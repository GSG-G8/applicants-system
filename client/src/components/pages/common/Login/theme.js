import { createMuiTheme } from '@material-ui/core/styles';

const Theme = createMuiTheme({
  palette: {
    primary: {
      main: 'rgb(237, 109, 35)',
    },
    secondary: {
      main: 'rgb(59,90,111)',
    },
    textPrimary: {
      main: 'rgb(33,36,41)',
    },
  },
});

export default Theme;
