const admin = require('./admin');
const applicant = require('./applicants');
const cohort = require('./cohorts');
const project = require('./projects');
const registrationStep = require('./steps');
const technicalTask = require('./tasks');

module.exports = {
  admin,
  applicant,
  cohort,
  project,
  registrationStep,
  technicalTask,
};
